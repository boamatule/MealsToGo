import * as React from 'react';
import { Searchbar } from 'react-native-paper';

export const SearchBar = () => {
  
  return (
    <Searchbar
      placeholder="Search"
      onChangeText={}
      value={}
    />
  );
};
