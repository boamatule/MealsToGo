import React from "react";
import { AccountBackground, AccountCover, AccountContainer } from "../component/account.styles";

export const AccountScreen = () => {
  return (
    <AccountBackground>
      <AccountCover />
      <AccountContainer>
        
      </AccountContainer>
    </AccountBackground>
  );
};
